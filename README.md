# Slack MathJax Renderer

This Chrome extension renders LaTeX expressions written as `\(...\)` and `\[...\]` in Slack Web.

## Supported delimiters

Inline math:

```text
\( x^2 + y^2 = z^2 \)
```

Display math:

```text
\[
  \sum_{i=1}^{n} i = \frac{n(n+1)}{2}
\]
```
